# Public Reconnect Verification, 2026-09-05

## Findings

- The public landing page served September 2 code that enabled automatic retries before initial authentication. Failed connections never settled, leaving saved-computer controls disabled.
- The current desktop server ID and public key match the reported offer. Its local device registry had zero active devices before QA. The latest revocations were September 5 around 16:38 Beijing time.
- The reported offer expired September 5 at 16:48:40 Beijing time. No offer or password is retained in this report.
- The relay health endpoint returned OK. Relay journal entries showed device-auth failures and invalid/reused pairing-token failures; the client-facing close reason was the generic `Server disconnected`.

## Deployment

- Applied only the local reconnect diff to the actual deployed `app.js` and `chisacode/session.js`, preserving other differences between production and repository HEAD.
- Updated the index app reference and the app session-module reference to cache stamp `20260905-connect-fix`.
- Three files published under `/var/www/dshd-app`: `chisacode/session.js`, `app.js`, `index.html`.
- Original hashes were checked before deployment, files staged and read back, then individually renamed into place. Backup: `/var/backups/dshd-connect-20260905-1740`.
- No relay restart, nginx configuration change, or restoration of revoked devices.

## Verification

- Mobile web unit tests: 271 passed.
- Controlled browser tests: 390px and 1280px connecting state, failure recovery, retained credentials, manual retry into chat passed.
- Public live QA against the existing desktop: revoked credentials rejected with usable controls; fresh offer paired and loaded the real host catalog; reopening without an offer reconnected with the saved secret. Final run passed all three checks.
- The dedicated QA device was revoked in cleanup. Browser state was temporary; credentials and conversation text were not captured in artifacts.
- One earlier live run authenticated on reconnect but timed out loading the host catalog after 30 seconds. The next full run passed. This intermittent timeout is not claimed fixed.
- Full fake-host browser suite: 24/29, with the same five failures on HEAD and the patched code (tool detail, slash popup, model rows, draft restore, Git branch sheet).

## User Recovery

Reload the public landing page to obtain the fix. Revoked saved-device credentials cannot be reused: refresh the pairing code in the desktop and open the new link or scan it. After successful pairing, the new saved secret supports subsequent reconnects. Never restore revoked permissions merely to make an old browser record connect.
