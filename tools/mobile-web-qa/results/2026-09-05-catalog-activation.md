# 目录瘦身：运行包启用与公网复测

日期：2026-09-05；Feature：`mobile-remote`。

## 授权和实际修改

用户允许临时更新本机远程模块并重启远程连接，随后在 Esc 停止操作后再次明确要求继续。只修改本机 `dist/win-unpacked/resources/app.asar.unpacked/src/shared/dshd-host-tunnel.js`；通过桌面远程面板正常关闭并开启，未强杀进程、未重启桌面主进程、未发布正式版本、未部署公网文件、未安装 APK。

- 原运行模块备份：`C:/Users/48818/AppData/Local/Temp/dshd-host-tunnel-pre-activation-20260905-195232.js`。
- 最终源码与运行模块 SHA-256 均为 `42c6c7549624e3044efee52a5432a5b39094fa079b136b3ba37f87c55cea7b56`。
- 继续操作后远程守护进程 PID 为 46824，父进程仍为桌面主进程 26680。
- 临时 `__qaHostMs` 与 `dshd_daemon_qa_catalog` 探针已从源码、磁盘运行模块和 QA 脚本移除，重启后不再加载诊断版本。

## 已定位问题

1. 原 session.list 响应约 507606 UTF-8 字节，已多次重现 30 秒超时。
2. 第一版保留五种投影，降到约 133723 字节，但真实公网仍有一次重连超时。不能把这版标成已解决。
3. 临时计时逐次对照：桌面取数约 2.75–4.78 秒；一次端到端 27.155 秒中桌面只占 3.122 秒。剩余长延迟主要不在桌面取数。
4. 中转机只读检查：服务 active、负载低、网卡队列没有报告丢包，但多个连接出现显著 TCP 重传。系统代理开启；仅在新 QA 浏览器中禁用代理的对照仍出现 24–27 秒，不能宣称代理是唯一原因。未修改服务器网络或系统代理配置。

## 最终目录契约

session.list 保留所有行、会话字段与投影元数据，只传 `title`、`sessionListMetadata` 两种投影。模型、权限和计划详情在打开会话的 history 中获取。history、创建、搜索等响应不受裁剪影响。未延长产品的 30 秒 RPC 超时。

## 本地验证

- 新增按需详情保留回归，目录断言先红后绿。
- Web + host tunnel/mux：304/304。
- 假 host 浏览器：30/30。

## 公网验证边界

使用真实公网 `/dshd/`、现有 Electron 所属 daemon 和真实中继。浏览器追加 RPC 耗时观测；同一次测试复用获取到的公网 bundle，避免探针对每次重开额外重复下载。页面总耗时不能当作无缓存冷启动指标。全部浏览器结果属于 Rehearsal，非 Android 实机或 T1 全功能 Pass。

最终版本第一次公网直连尝试在页面导航阶段达到 45 秒上限，没有取得目录样本；同一时段 curl 直连首页 HTTP 200，约 0.089 秒。

## 最终运行版测量结果

随后两组均使用未部署变更的公网 SPA，最终本机目录模块；每组包含撤销凭据拒绝、新 offer 配对、五次无 offer 保存电脑重连。

| 路径 | 新配对 session.list | 五次重连 session.list（毫秒） | 结果 |
| --- | --- | --- | --- |
| QA 浏览器直连 | 9248ms | 10602 / 9392 / 12197 / 4955 / 5363 | 6/6 目录加载成功 |
| 继承系统代理 | 5080ms | 5469 / 9952 / 8069 / 13476 / 6506 | 6/6 目录加载成功 |

- 每次返回 217 条会话、2 个工作区；session.list 响应为 62589–62590 UTF-8 字节，相比原 507606 字节约减少 87.7%。
- 12 次目录加载均没有错误横幅或 30 秒超时；耗时范围 4955–13476 毫秒。
- 两组撤销凭据均正确拒绝并恢复按钮；两组临时 QA 设备均在 finally 中撤销。
- 主进程仍为 26680，启动时间仍是 2026-09-05 16:35:12；本轮只重启远程。
- 最终语法检查、定向 `git diff --check` 通过。ADB 仍无连接设备，Android 实机仍 Blocked。

## 结论和剩余风险

目录超时的精简修复已在当前电脑运行包生效，两种路由共 12 次真实公网目录加载通过。未发布新版本，公网前端其余本地恢复修复仍未部署。公网资源加载曾出现单独的导航超时，链路仍有波动；本轮成功样本不构成任意弱网稳定性的保证，也不是 Android 实机或 T1 全量功能验收。

当前运行包会保留修复；将来重新构建或安装时须包含对应源码，不能恢复旧的目录模块。
