# 公网性能复测与实机验收状态

日期：2026-09-05。Feature：`mobile-remote`。

## 范围

用户恢复要求公网性能复测及实机验收，但未要求发布。测试使用正在运行的桌面和公网 `/dshd/`，没有部署、替换运行包、重启桌面或中继。浏览器为 Windows Edge headless、390 × 844；这属于真实链路 Rehearsal，不是 Android 实机或 T1 全量验收。

ADB `devices -l` 未列出设备，Android 实机验收 Blocked，等待手机连接并授权 USB 调试。

## 已观察结果

- 公网首页 HTTP 200，一次请求连接 0.002603 秒、首字节 0.101653 秒、总计 0.431353 秒；首页速度不能代替配对或目录加载速度。
- 未插入计时探针的公网浏览器：撤销凭据被拒绝后按钮恢复；新 offer 配对并加载目录成功；第一次保存电脑重连成功；第二次重连出现 `无法加载会话：Timeout waiting for message (30000ms)`，测试失败，未继续原定五轮。
- 第二轮只在浏览器拦截的公网 bundle 中追加耗时观测，不修改服务器文件或产品逻辑。新配对进入聊天共 57719 毫秒；`workspace.list` 333 毫秒、2403 UTF-8 字节；`session.list` 25280 毫秒、507606 UTF-8 字节。前一报告的字符串长度与本轮 UTF-8 字节数口径不同，不直接用于判断数据增长。
- 第二轮第一次保存电脑重连：`workspace.list` 588 毫秒、2403 字节；`session.list` 30002 毫秒后超时；从打开页面到显示目录错误 41375 毫秒。该轮也以失败结束，未继续原定三轮。
- 两轮临时 QA 设备均在 finally 中撤销；不保存配对令牌、设备密钥或会话正文。

## 版本边界

公网文件与本地源码指纹不同。桌面运行包仍为瘦身修复前模块，不能把本轮数据当作修复后性能。

| 文件 | 公网或运行包 SHA-256 | 本地修复源码 SHA-256 |
| --- | --- | --- |
| app.js | f438be499a8302609f9382b1cf8b845dd61d2320856cf89a402b7c2b7ee38488 | 21130a020af2679d6ef9df8707385056a3a4d82ce9cb29f0cdae7b348a22d763 |
| chisacode/session.js | ac304a30767a883f59a0b1f9797c773f21648c8d2d22172cb34dea8b2f94eac8 | 515132336f5590a79edf929bfffadc968c1f0633a5f68d16ad30835f2d69dc3e |
| dshd-host-tunnel.js | 2563f6f0196b7a2a3c2da3cf3c6f9a0ca0a495f9b0bdcd1449819336aa23959c | 172f683021d2b93db9612696a5167da9ac0c35e7ccbf1a392936bdd5fa49b4b |

## 判定

当前公网稳定性 Rehearsal **Fail**，已再次重现目录超时。修复版公网性能、Android 实机和 T1 全功能验收仍未通过。禁止把桌面浏览器手机尺寸测试写成实机验收。
