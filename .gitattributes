# Pairing records merge through the dshd-translation-pairing driver:
# auto-composed when both owner files merge cleanly, fail-closed otherwise.
*.i18n.yaml merge=dshd-translation-pairing

# Shell scripts and hooks must keep LF on Windows checkouts.
scripts/git-hooks/* text eol=lf
