# Metallic paint hover

Every native `button` in the main Web UI shows a translucent silver `linear-gradient` sheen on `:hover` — a layered `background-image` over the variant's own hover fill, never a replacement. Painted by the global `metallic-paint.css` sheet mounted via `installThemeStyles`; colors come only from `color-mix` on label aliases — bright bands `--dsw-alias-label-primary-foreground` (keeps glyph contrast), shades `--dsw-alias-label-primary`, mid `--dsw-alias-label-secondary` — so the sheen inverts with the theme and tints with custom themes (no literals, no theme branch). The 4.5s `background-position` sweep is a design value (motion.md indicator family); `prefers-reduced-motion` stops the sweep, keeps the static sheen. Scope stays native `<button>` in the app document — no `role='button'` rows, selects, inputs, boot/launcher/gallery windows, or mobile/web. The Appearance「按钮悬停光泽 / Button sheen」switch persists `metallicPaintEnabled` (default off) and flips `data-dsh-metallic-paint` on the document root — both rules key on that attribute, so off leaves only the variant hover fill. No WebGL/shader port; the single Appearance switch is the only control — no per-button or per-area toggles.

Full card: [docs/features/metallic-paint.md](../../docs/features/metallic-paint.md)
