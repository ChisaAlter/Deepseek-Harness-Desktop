# Agent Note：远程连接默认使用服务器

[English](2026-09-05-remote-server-default.md) | 中文

Status: implemented

## 决策

桌面将中继模式从「外出」改名为「服务器」，配置、配对地址选择与设置界面在缺少模式时统一默认 relay。明确选择的局域网模式仍然有效。配对开关、已保存的地址与传输协议不变。

## 理由

仅修改名称会让首次配对继续打开局域网页面。各消费方默认值一致，才能默认使用公网落地页，同时不强制切换现有局域网用户，也不自动开放远程访问。测试固定宿主配置归一化和界面选中项。
