# 请求链路修复验证

日期：2026-09-05。范围：当前工作树源码，不代表已发布版本或用户安装的 runtime 已更新。

## 已修复

- 识图设置重新被 agent 请求消费；上传、read_image、本地子 agent 图片跟进认可已配置的识图路由。
- 描述先记入 vision/describe，再进入主请求；重建只使用日志，不重新生成描述。补全生成的已知事件集合，避免描述事件阻塞历史读取。
- 识图鉴权失败、取消和超时不会派发主请求或保存半成品描述；超时保留 VISION_DESCRIBE_TIMEOUT。
- 流适配器、组装器和工具注册共享标识规则；畸形调用进入 MALFORMED_RESPONSE 恢复路径，不执行工具或提交无效 assistant 消息。
- 旧的畸形工具调用及关联结果仅从模型历史投影移除；原始日志和其他内容保持不变。
- 补齐冷历史测试夹具依赖；桌面 CI 增加核心集合和真实 dsh 无密钥恢复回放。

## 验证证据

| 检查 | 结果 |
| --- | --- |
| 核心行为集合：agent-loop/session/tools/llm/API/file/workspace/cache/subagent | 173 文件，3668 通过、3 跳过 |
| 追加关键回归：识图、请求校验、请求错误、历史投影、识图错误分类 | 5 文件，99 通过；含 7 项真实服务组合识图测试 |
| 宿主 build:lib:host | 通过，包含 TypeScript 编译和宿主库构建 |
| 新增 malformed-tool-call-retry | keyless refresh 和独立 replay 通过；真实 dsh 进程，无工具执行 |
| 桌面 CI/插件归因/恢复检查 | 33 通过；1 项因本机缺少 electron-builder/package.json 失败 |
| 文档双语配对 | 12 对检查通过 |

日志位于本机临时目录：dshd-fix-core.log、dshd-fix-host-final.log、dshd-fix-snapshot-replay.log。针对性测试计数与核心集合重叠，不累加。

## 尚未验收

未调用用户真实识图 API，未更新用户已安装 runtime，未生成或发布安装包，未宣称远端 CI 已通过。此次没有重跑 GUI 全集、双 SDK 快照或逐文件覆盖率门禁；此前审查的 GUI 通过结果不是本次新构建验收。

历史导入的工作区重新关联及启动插件错误归因保留本轮前已完成的修复；这不代表任意第三方插件私有数据格式均兼容。没有清空用户会话、插件数据或 API Key。

生成事件清单时，本机 src 下残留的忽略构建产物 .d.ts 导致标准扫描重复声明。本次使用同一生成器对源文件快照生成清单，未删除或改动这些本机产物；干净 CI 源码树不包含它们。
